Row for report lists — status badge follows the booking lifecycle; download appears only when ready.

```jsx
<ReportRow name="Lipid Profile" lab="HealthFirst" date="12 Jun" status="ready" onOpen={open} onDownload={dl} />
```
