Top app bar for every pushed screen — back chevron, heavy title, optional trailing action.

```jsx
<TopBar title="Book a slot" subtitle="HealthFirst Diagnostics" onBack={pop} action={<IconButton icon="phone" />} />
```
