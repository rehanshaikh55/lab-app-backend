Lucide-based line icon — the only icon source for Labzy UI; never hand-draw SVGs or use emoji.

```jsx
<Icon name="flask" size={20} color="var(--teal-500)" />
```

Names: search, map-pin, star, calendar, file-text, home, flask, user, bell, chevron-right/left/down, download, check, clock, x, plus, sliders, droplet, arrow-right, shield-check, navigation, credit-card, phone, repeat. Default 20px / 2px stroke; use 22–24px in tab bars, 16px inside small badges.
