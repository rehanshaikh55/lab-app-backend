Labzy wordmark as live text — use in app headers and splash; use the PNG assets (assets/brand/) for export contexts.

```jsx
<Logo size={28} />
<Logo size={28} tone="white" />  // on teal or dark surfaces
```
