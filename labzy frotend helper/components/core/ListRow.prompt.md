List row for settings, menus and grouped lists — soft teal icon tile, title + subtitle, trailing chevron.

```jsx
<ListRow icon="file-text" title="My reports" subtitle="12 documents" onClick={go} />
```
