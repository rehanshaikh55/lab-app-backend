Icon-only square button for top-bar actions (back, bell, filter) — keeps the 44px hit target.

```jsx
<IconButton icon="bell" label="Notifications" />
<IconButton icon="chevron-left" variant="ghost" label="Back" />
```
