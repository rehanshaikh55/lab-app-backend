Sunken segmented control — the white raised segment is selected. Use for collection mode, report filters.

```jsx
<SegmentedControl options={['Home collection', 'Walk-in']} value={mode} onChange={setMode} />
```
